
export async function askNidhi({ question }: { question: string }) {
    // Mock response for build
    console.log("Mock askNidhi called with:", question);
    return { answer: "I am a mock AI assistant. I cannot answer questions yet." };
}
